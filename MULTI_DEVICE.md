# 🖥️ Работа с проектом с разных устройств

Этот файл о том, как удобно работать с проектом дома, на работе, с ноутбука и не терять данные.

---

## 1. Что синхронизируется через GitHub

GitHub хранит:

- исходный код (Python, шаблоны, фронтенд);
- текстовую документацию (`*.md`, `*.json`);
- конфигурационные файлы (кроме `.env`).

GitHub **не хранит автоматически**:

- содержимое вашей локальной базы данных;
- файлы `.env` с паролями;
- загруженные картинки (если вы их не коммитите специально).

---

## 2. Три подхода к работе с разных ПК

### Подход 1. Облачная база данных (рекомендуется)

Идея: один экземпляр PostgreSQL в облаке, все устройства к нему подключаются.

Плюсы:

- все видят одни и те же задачи;
- не нужно переносить дампы базы;
- удобно для совместной работы.

Варианты сервисов:

- Railway;
- Render;
- ElephantSQL и т.п.

Схема:

1. На сервисе создаётся база PostgreSQL.
2. В `.env` вместо локальных параметров ставится `DATABASE_URL` облачной БД.
3. На каждом устройстве:
   - `git pull`;
   - настроен `.env` на ту же `DATABASE_URL`;
   - `python manage.py migrate`.

### Подход 2. Docker

Идея: поднимать контейнеры (PostgreSQL + приложение) одинаково на каждом ПК.

Плюсы:

- одинаковое окружение;
- не нужно отдельно ставить PostgreSQL.

Минусы:

- нужно уметь пользоваться Docker.

### Подход 3. Экспорт / импорт данных

Идея: таскать базу в виде JSON.

- На ПК A перед уходом:
- На ПК B:
  Плюс: просто. Минус: легко забыть сделать экспорт и перезаписать данные.

---

## 3. Общее правило работы

На **каждом** устройстве перед началом работы:
